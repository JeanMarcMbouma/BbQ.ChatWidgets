# Custom Action Handlers

Provide handlers via `IWidgetActionHandlerResolver` and register them early during service registration. Return structured responses so the widget list stays in sync with the chat state.
