# Usage

Typical usage patterns, endpoint descriptions, widget JSON contracts and examples. This page should reference `docs/api/` for detailed API signatures.

Sections:
- Endpoints and payloads
- Widget contracts (button, card, input, dropdown, slider, toggle, fileupload)
- Examples (request/response pairs)
