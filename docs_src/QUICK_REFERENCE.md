# Quick Reference

| Topic | Description |
| --- | --- |
| **API endpoints** | `/api/chat/message`, `/api/chat/action` |
| **Widgets** | Button, Card, Input, Dropdown, Slider, Toggle, FileUpload |
| **Customization** | Use `WidgetRegistry` and `WidgetActionHandlers` to hook into the UI |
| **Docs** | See `guides/`, `examples/`, `design/`, and the full `[API Reference](api/README.md)` |
