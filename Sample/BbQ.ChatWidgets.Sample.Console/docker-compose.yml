version: '3.8'

services:
  bbq-sample:
    build:
      context: .
      dockerfile: BbQ.ChatWidgets.Sample/Dockerfile
    
    environment:
      # Pass OpenAI API key
      - OpenAI__ApiKey=${OPENAI_API_KEY}
      - ASPNETCORE_ENVIRONMENT=Production
    
    stdin_open: true
    tty: true
    
    # For interactive mode
    # Remove these for background service
    # volumes:
    #   - ./logs:/app/logs

# To run:
# 1. Create .env file with: OPENAI_API_KEY=sk-...
# 2. docker-compose up
